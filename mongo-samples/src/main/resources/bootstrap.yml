spring:
  application:
    name: private-mongo-service
  profiles:
    active: local4.x
  main:
    allow-bean-definition-overriding: true
logging:
  level:
    com:
      alibaba:
        nacos: error